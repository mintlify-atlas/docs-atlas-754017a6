---
title: "Quickstart"
description: "Get Port Whisperer running in under a minute."
---

<Steps>
  <Step title="Install Port Whisperer">
    ```bash
    npm install -g port-whisperer
    ```
  </Step>

  <Step title="List your active dev ports">
    ```bash
    ports
    ```

    Port Whisperer scans all TCP listening ports and shows a color-coded table of dev servers, databases, and Docker containers:

    ```
     ┌─────────────────────────────────────┐
     │  🔊 Port Whisperer                  │
     │  listening to your ports...         │
     └─────────────────────────────────────┘

    ┌───────┬─────────┬───────┬──────────────────────┬────────────┬────────┬───────────┐
    │ PORT  │ PROCESS │ PID   │ PROJECT              │ FRAMEWORK  │ UPTIME │ STATUS    │
    ├───────┼─────────┼───────┼──────────────────────┼────────────┼────────┼───────────┤
    │ :3000 │ node    │ 42872 │ frontend             │ Next.js    │ 1d 9h  │ ● healthy │
    ├───────┼─────────┼───────┼──────────────────────┼────────────┼────────┼───────────┤
    │ :5432 │ docker  │ 58351 │ backend-postgres-1   │ PostgreSQL │ 10d 3h │ ● healthy │
    └───────┴─────────┴───────┴──────────────────────┴────────────┴────────┴───────────┘

      2 ports active  ·  Run ports <number> for details  ·  --all to show everything
    ```
  </Step>

  <Step title="Inspect a specific port">
    Pass a port number to see full details — process tree, git branch, memory, and more:

    ```bash
    ports 3000
    ```

    Port Whisperer will also offer an interactive prompt to kill the process if needed.
  </Step>

  <Step title="Kill a stuck process">
    Free up a port in one command:

    ```bash
    ports kill 3000
    ```

    Use `-f` to force kill with SIGKILL if the process doesn't respond:

    ```bash
    ports kill -f 3000
    ```
  </Step>
</Steps>

## Next steps

<CardGroup cols={2}>
  <Card title="Full port listing" icon="list" href="/commands/ports">
    Show all ports with filtering options
  </Card>
  <Card title="Port inspection" icon="magnifying-glass" href="/commands/inspect">
    Deep-dive into any process on a port
  </Card>
  <Card title="Process list" icon="rectangle-list" href="/commands/ps">
    View all running dev processes
  </Card>
  <Card title="Docker guide" icon="docker" href="/guides/docker">
    Working with Docker containers
  </Card>
</CardGroup>
